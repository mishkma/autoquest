# AutoQuest — Roadmap

Геймифицированный курс Python «с нуля» для перехода в автоматизацию тестирования — от печати первой строки до фреймворка автотестов на реальном UI+API проекте.

- Репозиторий: https://github.com/mishkma/autoquest (ветка `master`)
- Сайт: https://mishkma.github.io/autoquest/ (GitHub Pages, деплой автоматически при пуше в `master`)
- Наглядная версия этой роадмапы (Claude Artifact): https://claude.ai/code/artifact/bb7fbbad-f3b5-4ad5-8c53-11d57b4416f4

**Прогресс: 5 / 17 модулей готовы.**

## Модули

### Этап 1 — База Python

| # | Модуль | Статус |
|---|---|---|
| 1 | Первый код и переменные | готово |
| 2 | Условия | готово |
| 3 | Циклы | готово |
| 4 | Функции | готово |
| 5 | Списки и словари | готово |
| 6 | Установка окружения (VS Code, настоящий Python) | **дальше** |
| 7 | Git для автотестов | план |
| 8 | ООП: классы и объекты | план |
| 9 | Стандартная библиотека и генераторы | план |

### Этап 2 — Автотесты на Python

| # | Модуль | Статус |
|---|---|---|
| 10 | Pytest | план |
| 11 | Моки и стабы (+ чтение ошибок/отладка) | план |
| 12 | CI/CD для автотестов | план |

### Этап 3 — Инструменты автоматизации

| # | Модуль | Статус |
|---|---|---|
| 13 | API-тестирование | план |
| 14 | Базы данных | план |
| 15 | Локаторы и Selenium | план |
| 16 | Playwright | план |
| 17 | Архитектура фреймворка (AutoQuest Test Framework целиком) | план |

## Платформа

- Структура: `index.html` + `css/style.css` + `js/interpreter.js` (свой Python-движок на JS, без Pyodide) + `js/modules-data.js` (контент модулей) + `js/app.js` (UI/состояние)
- Прогресс студента хранится в `localStorage` браузера (ключ `autoquest-state`)
- Тема сквозного проекта (AutoQuest Test Framework) — с модуля 4, реальный репозиторий/git/CI — с модуля 6+

### Ограничения интерпретатора (важно при написании новых заданий)

- списки/словари-литералы — только в одну строку, без переноса
- нет срезов (`x[1:3]`), только одиночный индекс (в т.ч. отрицательный)
- нет общего `in`, только `for ... in`
- у словаря нет `.items()` — только `.get()/.keys()/.values()`
- `min/max/.sort()` — только по числам
- функции без default-параметров, `*args`/`**kwargs`, multiple return

## Бэклог

- **план** — Автодеплой: пуш пока делает пользователь вручную из терминала (облачная песочница и рабочая сеть блокируют прямой push к GitHub). Так как сайт — чистая статика на GitHub Pages без сборки, отдельный workflow, скорее всего, не нужен: Pages и так публикует сайт на каждый пуш в `master` автоматически — уточняется.
- **план** — Перевести всё содержимое курса на английский (сейчас теория на русском, задания на английском).
- **после перевода** — Добавить переключаемую локализацию на русский поверх английской версии.
- **после готовности всех 17 модулей** — 8-битный визуальный стиль (модули = уровни, боссы = боссы игры; референс палитры/HUD — Contra/Battletoads/Double Dragon). Мини-превью уже одобрен (HUD, карточки уровней), блок кода нужно передумать под более «естественный» разработческий вид.
- **после готовности курса** — Доступ для друга без claude.ai (вероятно решится само собой — сайт теперь на GitHub Pages, просто дать ссылку).
- **готово** — Апгрейд редактора кода: номера строк, подсветка синтаксиса и строки с ошибкой, автодополнение.

---
*Обновляется вручную по ходу разработки. Последнее обновление: 8 сентября 2026.*
